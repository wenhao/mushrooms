spring:
  application:
    name: mock-server
  redis:
    host: localhost
    port: 6379
    password:
    lettuce:
      pool:
        max-active: 8
        max-idle: 8
        max-wait: -1ms
        min-idle: 1
server:
  port: 8080

parrot:
  cache:
    enabled: true
    key: CACHE
    headers:
      - host
