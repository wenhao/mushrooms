spring:
  application:
    name: mock-server
  redis:
    host: localhost
    port: 6379
    password:
    lettuce:
      pool:
        max-active: 8
        max-idle: 8
        max-wait: -1ms
        min-idle: 1
server:
  port: 8080

feign:
  hystrix:
    enabled: false
  okhttp:
    okhttp: true
  client:
    config:
      default:
        connectTimeout: 5000
        readTimeout: 5000
        loggerLevel: full

mushrooms:
  resttemplate:
    enabled: true
  okhttp:
    enabled: true
  key: PARROT-CACHE
  headers:
    - host
